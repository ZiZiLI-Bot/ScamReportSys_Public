"use client";
import { useAuth } from "@/hooks/useAuth.hook";
import { Button } from "antd";
// import { view_token } from "./actions";

export default function ReportPage() {
  return (
    <main>
      <p>REposet</p>
      {/* <Button onClick={view_token}>VIEW token</Button> */}
    </main>
  );
}
